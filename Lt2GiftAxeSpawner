local player = game.Players.LocalPlayer
local mouse = player:GetMouse()

-- GUI Kurulumu
local ScreenGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
ScreenGui.Name = "ItemSpawner"

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 250, 0, 350)
MainFrame.Position = UDim2.new(0.5, -125, 0.5, -175)
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true

local UICorner = Instance.new("UICorner", MainFrame)
UICorner.CornerRadius = UDim.new(0, 12)

-- X Butonu
local CloseBtn = Instance.new("TextButton", MainFrame)
CloseBtn.Size = UDim2.new(0, 60, 0, 30)
CloseBtn.Position = UDim2.new(0.5, -65, 1, -35)
CloseBtn.Text = "X"
CloseBtn.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 20

local CloseUICorner = Instance.new("UICorner", CloseBtn)
CloseUICorner.CornerRadius = UDim.new(0, 8)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Auto Button
local autoEnabled = false

local AutoBtn = Instance.new("TextButton", MainFrame)
AutoBtn.Size = UDim2.new(0, 60, 0, 30)
AutoBtn.Position = UDim2.new(0.5, 5, 1, -35)
AutoBtn.Text = "Auto"
AutoBtn.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
AutoBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
AutoBtn.Font = Enum.Font.SourceSansBold
AutoBtn.TextSize = 20

local AutoUICorner = Instance.new("UICorner", AutoBtn)
AutoUICorner.CornerRadius = UDim.new(0, 8)

-- Gift ve Axe listeleri
local Axes = {
    "Select Axe","BasicHatchet","ManyAxe","Axe1","Axe2","Axe3","SilverAxe","AxeBetaTesters",
    "EndTimesAxe","AxeChicken","CandyCaneAxe","AxeAmber","GingerBreadAxe","AxeTwitter","RustyAxe"
}

local Gifts = {
    "Select Gift","2016CGift_Ut","2015CGift_Coal","2015CGift_Red","2016CGift_Sweet","2015CGift_Volcano",
    "2015CGift_Wobble","2016CGift_Blue","2016CGift_Big","2016CGift_Jingle","2016CGift_Wobble",
    "2018CGift_Wobble","2017CGift_Green","2017CGift_GreatTimes","2017CGift_Modern","2017CGift_Wobble",
    "2019CGift_Wobble_","2017CGifts_Gold","2018CGift_Snow","2018CGift_Plate","2018CGift_Cocoa",
    "2018CGift_Candy","2018CGift_Wobble_","2018CGift_Duck","2018CGift_Cone","2018CGift_Sled",
    "2018CGift_GingerAxe","2018CGift_Plum","2019CGift_Yellow_","2019CGift_Bowl","2019CGift_Rusty",
    "2019CGift_Cola","2019CGift_Burnt","2020CGift_Teal","2020CGift_Wobble","2020CGift_Cave",
    "2021CGift_Black","2021CGift_Wobble"
}

-- Seçili item
local selection = nil
local Amount1 = 5

-- Spawn fonksiyonu
function spawn()
    local Item = selection
    local Amount = Amount1
    local Plr = game:GetService("Players").LocalPlayer
    local Added

    Added = game:GetService("Workspace").PlayerModels.ChildAdded:Connect(function(v)
        local Owner, Type = v:WaitForChild("Owner"), v:WaitForChild("Type")
        if Owner.Value == Plr and Type.Value == "Blueprint" then
            game:GetService("ReplicatedStorage").PlaceStructure.ClientPlacedStructure:FireServer(Item, Plr.Character.Head.CFrame, Plr, nil, v, true)
        end
    end)

    for i = 1, Amount do
        game:GetService("ReplicatedStorage").PlaceStructure.ClientPlacedBlueprint:FireServer("Floor1Tiny", Plr.Character.Head.CFrame, Plr)
        task.wait()
    end

    task.wait(2)
    Added:Disconnect()
end

-- Auto sistem
AutoBtn.MouseButton1Click:Connect(function()
    autoEnabled = not autoEnabled
    if autoEnabled then
        AutoBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
        task.spawn(function()
            while autoEnabled do
                if selection and selection ~= "Select Gift" and selection ~= "Select Axe" then
                    spawn()
                end
                task.wait(1.5)
            end
        end)
    else
        AutoBtn.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
    end
end)

-- Gift Button
local GiftBtn = Instance.new("TextButton", MainFrame)
GiftBtn.Size = UDim2.new(0, 220, 0, 30)
GiftBtn.Position = UDim2.new(0, 15, 0, 50)
GiftBtn.Text = "Spawn Random Gift"
GiftBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
GiftBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
GiftBtn.Font = Enum.Font.SourceSans
GiftBtn.TextSize = 18

GiftBtn.MouseButton1Click:Connect(function()
    local index = math.random(2, #Gifts)
    selection = Gifts[index]
    spawn()
end)

-- Axe Button
local AxeBtn = Instance.new("TextButton", MainFrame)
AxeBtn.Size = UDim2.new(0, 220, 0, 30)
AxeBtn.Position = UDim2.new(0, 15, 0, 90)
AxeBtn.Text = "Spawn Random Axe"
AxeBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
AxeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
AxeBtn.Font = Enum.Font.SourceSans
AxeBtn.TextSize = 18

AxeBtn.MouseButton1Click:Connect(function()
    local index = math.random(2, #Axes)
    selection = Axes[index]
    spawn()
end)
