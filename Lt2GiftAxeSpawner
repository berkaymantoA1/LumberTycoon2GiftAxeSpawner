local player = game.Players.LocalPlayer
local mouse = player:GetMouse()

-- GUI Kurulumu
local ScreenGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
ScreenGui.Name = "ItemSpawner"

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 250, 0, 350)
MainFrame.Position = UDim2.new(0.5, -125, 0.5, -175)
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true

local UICorner = Instance.new("UICorner", MainFrame)
UICorner.CornerRadius = UDim.new(0, 12)

-- X Butonu (Ortada ve çalışır şekilde)
local CloseBtn = Instance.new("TextButton", MainFrame)
CloseBtn.Size = UDim2.new(0, 60, 0, 30)
CloseBtn.Position = UDim2.new(0.5, -30, 1, -35)
CloseBtn.Text = "X"
CloseBtn.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.SourceSansBold
CloseBtn.TextSize = 20

local CloseUICorner = Instance.new("UICorner", CloseBtn)
CloseUICorner.CornerRadius = UDim.new(0, 8)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Fare ile taşıma için Holding kontrolü
local Holding = Instance.new("BoolValue", MainFrame)
Holding.Name = "Holding"
Holding.Value = false

mouse.Button1Down:Connect(function()
    Holding.Value = true
end)

mouse.Button1Up:Connect(function()
    Holding.Value = false
end)

mouse.Move:Connect(function()
    if Holding.Value == true then
        MainFrame.Position = UDim2.new(0, mouse.X, 0, mouse.Y)
    end
end)

-- ScrollingFrame ve butonlar
local ScrollingFrame = Instance.new("ScrollingFrame", MainFrame)
ScrollingFrame.Size = UDim2.new(1, -10, 1, -50)
ScrollingFrame.Position = UDim2.new(0, 5, 0, 5)
ScrollingFrame.BackgroundTransparency = 1
ScrollingFrame.BorderSizePixel = 0
ScrollingFrame.ScrollBarThickness = 6

-- Item Listeleri
local Gifts = {
    "2016CGift_Ut", "2015CGift_Coal", "2015CGift_Red", "2016CGift_Sweet", "2015CGift_Volcano",
    "2015CGift_Wobble", "2016CGift_Blue", "2016CGift_Big", "2016CGift_Jingle", "2016CGift_Wobble",
    "2018CGift_Wobble", "2017CGift_Green", "2017CGift_GreatTimes", "2017CGift_Modern", "2017CGift_Wobble",
    "2019CGift_Wobble_", "2017CGifts_Gold", "2018CGift_Snow", "2018CGift_Plate", "2018CGift_Cocoa",
    "2018CGift_Candy", "2018CGift_Wobble_", "2018CGift_Duck", "2018CGift_Cone", "2018CGift_Sled",
    "2018CGift_GingerAxe", "2018CGift_Plum", "2019CGift_Yellow_", "2019CGift_Bowl", "2019CGift_Rusty",
    "2019CGift_Cola", "2019CGift_Burnt", "2020CGift_Teal", "2020CGift_Wobble", "2020CGift_Cave",
    "2021CGift_Black", "2021CGift_Wobble"
}

local Axes = {
    "BasicHatchet", "ManyAxe", "Axe1", "Axe2", "Axe3", "SilverAxe", "AxeBetaTesters", "EndTimesAxe",
    "AxeChicken", "CandyCaneAxe", "AxeAmber", "GingerBreadAxe", "AxeTwitter", "RustyAxe"
}

ScrollingFrame.CanvasSize = UDim2.new(0, 0, 0, (#Gifts + #Axes + 2) * 45)

local function spawnItem(itemName)
    local Plr = game.Players.LocalPlayer
    local Added = workspace.PlayerModels.ChildAdded:Connect(function(v)
        local Owner = v:WaitForChild("Owner")
        local Type = v:WaitForChild("Type")
        if Owner.Value == Plr and Type.Value == "Blueprint" then
            game.ReplicatedStorage.PlaceStructure.ClientPlacedStructure:FireServer(itemName, Plr.Character.Head.CFrame, Plr, nil, v, true)
        end
    end)
    for i = 1, 5 do
        game.ReplicatedStorage.PlaceStructure.ClientPlacedBlueprint:FireServer("Floor1Tiny", Plr.Character.Head.CFrame, Plr)
        task.wait()
    end
    task.wait(2)
    Added:Disconnect()
end

local function createLabel(text, yPos)
    local Label = Instance.new("TextLabel", ScrollingFrame)
    Label.Size = UDim2.new(1, -20, 0, 30)
    Label.Position = UDim2.new(0, 10, 0, yPos)
    Label.BackgroundTransparency = 1
    Label.Text = text
    Label.Font = Enum.Font.SourceSansBold
    Label.TextSize = 20
    Label.TextColor3 = Color3.fromRGB(255, 255, 255)
    return yPos + 35
end

local function createButton(name, yPos)
    local Button = Instance.new("TextButton", ScrollingFrame)
    Button.Size = UDim2.new(1, -20, 0, 40)
    Button.Position = UDim2.new(0, 10, 0, yPos)
    Button.Text = name
    Button.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.Font = Enum.Font.SourceSansBold
    Button.TextSize = 16

    local UICornerBtn = Instance.new("UICorner", Button)
    UICornerBtn.CornerRadius = UDim.new(0, 6)

    Button.MouseButton1Click:Connect(function()
        spawnItem(name)
    end)
    return yPos + 45
end

local Y = createLabel("Gifts", 10)
for _, gift in ipairs(Gifts) do
    Y = createButton(gift, Y)
end

Y = createLabel("Axes", Y)
for _, axe in ipairs(Axes) do
    Y = createButton(axe, Y)
end

game.StarterGui:SetCore("SendNotification", {
    Title = "Berk Spawner",
    Text = "GUI Ready!",
    Duration = 5
})
